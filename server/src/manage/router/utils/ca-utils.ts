import Moment from 'moment';
import { extendMoment } from 'moment-range';
const moment = extendMoment(Moment as any);
import { paramUtils } from './param-utils';

async function formatDimensions() {}

export const caUtils = {
  formatDimensions
};
